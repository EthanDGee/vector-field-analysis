# Simulator Docs

Documentation for the `simulator` binary.

## Contents

- **Generation algorithms** — how vector fields are procedurally constructed
- **Configuration options** — parameters that control field dimensions, magnitude, and pattern
- **Output formats** — formats the simulator can produce for consumption by the analyzer or other tools
- **Usage examples** — example invocations and generated field walkthroughs
