# Analyzer Docs

Documentation for the `analyzer` binary.

## Contents

- **Input formats** — supported vector field data formats and how to feed data into the analyzer
- **Algorithms** — how streamline computation works, flow tracing via `flowFromVector`, and streamline merging
- **Usage examples** — example invocations and code walkthroughs
